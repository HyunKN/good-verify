# good-verify Design

이 문서는 UI 구현·리뷰의 기준이다. 사용자 요구 → 이 문서 → 컴포넌트 관례 순으로 적용한다. 마지막 자료 확인: 2026-09-22. 아래 외부 자료는 공개된 지침이며 각 회사의 비공개 개발 프로세스를 확인했다는 뜻은 아니다.

## 1. 제품에 맞는 방향

**시각 방향:** 장식보다 기록을 읽고 판단하기 쉬운, 차분한 로컬 작업 도구.

**정보 방향:** 현재 프로젝트와 코드 상태 → 해야 할 일 → 기대값과 실제값 → 근거 → 상세 로그. 첫 화면도 마케팅 hero가 아니라 프로젝트 연결이나 실제 작업을 보여준다.

**상호작용 방향:** 조작 결과를 가까운 위치에 표시한다. 저장 중 중복 실행을 막고, 취소와 돌아가기 경로를 제공한다. 전환은 사용자의 위치를 설명할 때만 사용한다. 중요한 검증 상태를 잠깐 나타나는 toast에만 맡기지 않는다.

이 제품의 개성은 과장된 그래픽이 아니라 **사실·가설·원인, 실행 성공·검증 완료, 현재 코드·과거 근거를 명료하게 구분하는 것**이다.

## 2. 기본 금지 패턴

기능적 이유가 없으면 다음 패턴은 사용하지 않는다. 사용자 지정 금지사항이며 외부 가이드의 취향으로 완화하지 않는다.

| 패턴                              | 기본 대안                          | 예외 판단                                                  |
| --------------------------------- | ---------------------------------- | ---------------------------------------------------------- |
| Side Accent Bar                   | 간격·제목·전체 행의 옅은 선택 배경 | 장식용 측면 색띠는 사용하지 않는다                         |
| Side-Tab Card                     | 목록과 구분선, 평평한 상세 영역    | 실제 탭은 `tablist` 동작과 내용 전환이 필요할 때만         |
| 반복 pill badge                   | 상태를 짧은 텍스트로 표시          | 필터 제거 같은 실제 상호작용이 있는 토큰만 별도 검토       |
| 모든 내용을 둥근 card grid에 배치 | 목록·표·섹션·분할 작업 영역        | 카드 자체가 선택하거나 비교할 하나의 독립 객체일 때만      |
| Glassmorphism·과한 blur           | 불투명 surface, 명료한 경계        | 현재 제품에는 사용하지 않는다                              |
| glow·neon·장식 gradient           | 중립색과 제한된 강조색             | 현재 제품에는 사용하지 않는다                              |
| 이모지                            | 구체적인 한국어 레이블             | 사용하지 않는다. `+`, 화살표 등 기능 기호는 꼭 필요할 때만 |
| 의미 없는 아이콘 타일             | 이름·설명·행동을 직접 표시         | 의미를 전달하는 소형 아이콘도 텍스트를 대체하지 않는다     |
| 반복되는 세 카드 구성             | 실제 데이터 구조에 맞는 목록·표    | 정보가 세 개라는 이유만으로 카드로 만들지 않는다           |

타임라인의 연결선, 열 사이의 중립 구분선, 키보드 focus outline은 장식용 Side Accent Bar가 아니다. 각각 순서·영역·포커스를 전달할 때만 사용한다. **선택된 세션에 붙이는 녹색 세로줄, 보고서 표지의 색띠, 경고의 측면 색띠는 사용하지 않는다.**

Impeccable은 측면 색띠, 반복 카드, 과한 효과, 중복 레이블, 작은 텍스트 등을 구분해서 검토한다. 이 프로젝트는 탐지 도구 통과를 미적 품질의 증명으로 삼지 않고, 실제 작업 맥락과 렌더링을 함께 검토한다. [Impeccable Slop](https://impeccable.style/slop/)

## 3. 공식 자료에서 가져온 원칙과 적용 경계

### Apple: 명확한 위계와 사용자의 통제

HIG의 레이아웃 지침은 관련 내용을 묶고 핵심 정보에 공간을 주며, 다양한 화면과 글자 크기에서도 관계를 유지하도록 안내한다. 피드백의 강도는 정보의 중요도에 맞춰야 한다. [Layout](https://developer.apple.com/design/human-interface-guidelines/layout), [Feedback](https://developer.apple.com/design/human-interface-guidelines/feedback)

우리의 적용: 기대값/실제값을 먼저, 로그는 펼침 영역에 둔다. 기록 시작과 종료를 명확히 구분한다. 파괴적 동작은 확인하고, 단순 조회는 방해하지 않는다. OS의 Liquid Glass 외관은 이 웹 도구에 복제하지 않는다. 네이티브 플랫폼의 pt 수치를 웹 px에 그대로 옮기지도 않는다.

### OpenAI: 작업에 집중하는 시각 언어

공개 Plugins UI 지침은 일관된 색·글자·간격으로 혼란을 줄이고 과도한 gradient나 배경 패턴을 피하도록 설명한다. 이 자료는 ChatGPT 내 도구를 위한 것이므로 독립 앱 전체에 의무 적용되는 규격이 아니다. [OpenAI UI guidelines](https://developers.openai.com/plugins/concepts/ui-guidelines)

우리의 적용: 하나의 주요 행동, 설명적인 레이블, 결과 근처의 피드백을 사용한다. 주 행동은 검정으로 강조하고, 상태는 명시적인 텍스트로 구분한다. ChatGPT 로고·폰트·색을 복제하거나 OpenAI와의 제휴를 암시하지 않는다. AI가 쓰는 API도 사람이 읽는 것과 같은 근거를 사용한다.

### Vercel: 실제로 작동하는 세부 상호작용

Web Interface Guidelines는 키보드 동작, 보이는 focus, focus 이동과 복귀, 입력값 보존, zoom, 로딩 상태 등을 다룬다. Geist는 재사용 가능한 디자인 시스템을 제공한다. [Web Interface Guidelines](https://vercel.com/design/guidelines), [Geist](https://vercel.com/geist/introduction)

우리의 적용: 버튼은 실제 button, 이동은 link, 입력은 연결된 label을 사용한다. 저장 중 기존 버튼 이름을 유지한다. 모달의 Tab 순환·Escape·닫은 뒤 focus 복귀를 검증한다. 오류가 나도 작성한 내용을 버리지 않는다. Vercel의 검정 화면이나 컴포넌트를 외형만 모방하지 않는다.

### Meta: 접근성을 반복 가능한 기반으로

Meta의 공개 웹 접근성 사례는 공통 컴포넌트, 키보드 동작, focus 관리, 상태 알림을 기반에 넣는 방식을 설명한다. 2020년의 공개 사례이며 현재 내부 도구나 조직 운영을 확인한 자료는 아니다. [Building greater accessibility into Facebook.com](https://engineering.fb.com/2020/07/30/web/facebook-com-accessibility/)

우리의 적용: 접근성을 마지막 체크리스트로만 취급하지 않는다. 공통 입력·대화상자·상태 표시에 적용하고 실제 사용자 흐름에서 검사한다. QA 도구 자체가 키보드로 쓸 수 있어야 한다. 라이브 로그 전체를 매번 읽어 주지 않고 중요한 전환만 알린다.

## 4. 작업 공간과 화면별 계약

| 화면          | 먼저 보이는 것                           | 주요 행동                  | 상세 정보                           |
| ------------- | ---------------------------------------- | -------------------------- | ----------------------------------- |
| 프로젝트·작업 | 프로젝트, 요구사항, 완료 기준, 남은 확인 | 작업 만들기 또는 기준 수정 | 검사 명령, 리뷰 근거                |
| QA 기록       | 기록 중 여부, 현재 세션, 조작 순서       | 시작 또는 종료             | 네트워크·콘솔·수집 범위             |
| 발견 상세     | 기대 결과와 실제 결과                    | 재현 정리                  | 사실·가설·원인, 화면·로그           |
| 시나리오      | 이름, 버전, 기대 결과, 초안 여부         | 재실행                     | 파생 단계, 준비 조건, 코드 내보내기 |
| 실행 결과     | 결과, 현재/과거 코드 구분, 다음 행동     | 재시도·취소·수동 확인      | 단계별 결과, 당시 테스트 정의       |
| 보고서        | 범위, 판정, 미검증 사항                  | 열기 또는 명시적 내보내기  | 첨부 선택·미리보기                  |

앱 전역 탐색용 sidebar는 허용한다. 이것은 Side-Tab Card가 아니다. 좁은 화면에서는 가로 탐색과 단일 열로 바꾸되 본문 전체가 옆으로 밀리면 안 된다. 한 화면의 위계를 설명하기 위해 카드와 제목을 여러 겹 중첩하지 않는다.

일반 목록의 배경은 본문과 같게 둔다. 선택한 행만 옅은 중립 배경과 글자 굵기로 구분하고 `aria-current` 또는 적절한 선택 의미를 부여한다. 데이터 수가 증가해도 레이아웃을 유지한다. 0개·1개·긴 이름·많은 기록을 별도로 확인한다.

## 5. 시각 토큰

아래 수치는 good-verify의 자체 결정이며 위 회사들의 공통 규격이라는 뜻이 아니다.

- Surface: `#ffffff` 기본, 흰색 입력/대화상자, 중립 회색 보조 영역. 모든 UI 색상은 무채색이다.
- Text: `#171717` 주요, `#5f5f5f` 보조. 실제 배경과의 대비를 확인한다.
- Border: `#e0e0e0`, 두께 1px. 선택 강조를 위해 한쪽만 3–4px로 늘리지 않는다.
- Action: `#171717` 기반 검정 주요 버튼. 상태는 색 대신 통과·실패·수동 확인 텍스트로 구분한다.
- Radius: 일반 컨트롤 4–6px, 대화상자 최대 8px. 목록·보고서 섹션은 0. 원형은 실제 점 표시 같은 기하학적 의미에만 쓴다.
- Spacing: 4 / 8 / 12 / 16 / 24 / 32 / 48px를 우선한다. 공백은 정보 묶음을 구분하기 위해 사용한다.
- Typography: `Segoe UI`, `Malgun Gothic`, `system-ui`, `sans-serif`. 오프라인 동작과 한글 가독성을 위해 시스템 폰트를 선택한다. 외부 폰트 요청은 하지 않는다.
- 본문 14–16px, 메타데이터 12px 이상, 섹션 제목 18–22px, 페이지 제목 28–32px. 버튼·입력의 글자를 장식적 소문자로 축소하지 않는다.
- 코드는 고정폭 글꼴. 긴 URL·해시는 줄바꿈하거나 명시적으로 복사 가능하게 한다. 핵심 기대값은 줄임표로 숨기지 않는다.
- Motion: 버튼 반응 150ms. 메뉴 이동은 이전 본문 fade-out 160ms → 다음 본문 fade-in 160ms로 순차 전환하며 탐색 영역은 유지한다. 대화상자는 배경 fade 180ms와 300ms의 98%→100% scale/8px 이동을 함께 사용하고 닫힐 때도 퇴장 후 제거한다. 데이터 갱신에는 재시작하지 않는다. 빠른 재탐색은 마지막 요청을 따르며 퇴장 중 본문은 inert 처리한다. 대화상자 퇴장 완료까지 배경 조작을 막고 이후 포커스를 복귀한다. `prefers-reduced-motion`은 Web Animations API에도 적용하며 실행 중 설정 변경에도 즉시 완료한다. 펄스·자동 반복 효과는 사용하지 않는다.
- 전환 구현: 퇴장 애니메이션이 끝난 뒤에 다음 화면을 등장시킨다. 외부 애니메이션 라이브러리를 추가하지 않고 Web Animations API 기반의 자체 hook으로 처리한다. 접이식 sidebar나 단계형 wizard처럼 이 제품에 없는 구조를 전제하지 않는다. 프로젝트 선택은 비동기 데이터 전환이므로 이 메뉴 애니메이션과 별개다.
- Gloss: 사용자 요청(2026-09-23)에 따라 주요 버튼에만 검정 명암 gradient와 얇은 inset 반사선을 허용한다. 화면 배경·목록에는 적용하지 않으며 glow·blur·glass는 여전히 금지한다.

## 6. 상태와 문장

`통과`, `실패`, `실행 불가`, `취소`, `수동 확인 필요`를 정확히 사용한다. `기록 종료`는 검증 통과가 아니다. `재생 성공`도 완료 기준 충족이 아니다. `검증 완료`는 공통 완료 판정의 결과일 때만 표시한다.

- 원인을 모르면 “원인 미확정”. 자동으로 “네트워크 문제”라고 확정하지 않는다.
- 오류 문구는 “무엇이 실패했는지 → 보존된 내용 → 다음 행동” 순서로 쓴다.
- 성공 toast는 보조 수단이다. 저장 위치·실행 결과처럼 다시 찾아야 할 정보는 해당 화면에도 남긴다.
- 비활성화된 주요 행동에는 이유를 가까이 표시한다. 작성자가 충분한 완료 기준을 정하지 않았다고 도구가 임의로 정답을 생성하지 않는다.
- 빈 화면은 빈 이유와 가능한 다음 행동을 보여준다. 가짜 숫자·예시 성공률로 채우지 않는다.
- 중복 영어 eyebrow와 한국어 제목을 반복하지 않는다. 수식어보다 프로젝트·작업·코드 식별 정보가 유용하다.

## 7. 접근성과 반응형 검수

자체 목표는 일반 텍스트 대비 4.5:1, 큰 텍스트 및 의미 있는 컨트롤 경계 3:1이다. 실제 측정 없이 준수했다고 선언하지 않는다. 관련 기준: [WCAG 2.2](https://www.w3.org/TR/WCAG22/).

- 모든 핵심 흐름을 키보드로 실행하고 focus가 항상 보이는지 확인한다.
- 입력은 placeholder가 아니라 label로 식별한다. 오류를 관련 입력과 연결한다.
- 모달은 이름, focus 진입·순환·복귀, Escape, 배경 조작 차단이 필요하다. 여러 열의 복잡한 설정을 작은 모달에 압축하지 않는다.
- 비동기 상태는 적절한 `role=status`/`aria-live`로 알린다. 실패는 사용자가 처리할 때까지 남긴다.
- 모바일 주 컨트롤은 44px 터치 영역을 목표로 한다. 입력 글자 크기는 최소 16px. 브라우저 zoom을 막지 않는다.
- 390px, 768px, 1280px 이상과 200% 확대에서 읽기·조작·줄바꿈을 확인한다.
- 캡처 이미지는 링크 이름과 주변 설명으로 내용을 이해할 수 있어야 한다. 이미지의 색만 근거로 판정하지 않는다.

## 8. 구현·리뷰 절차

1. 변경 전 이 문서를 읽고 화면의 주요 작업과 상태를 한 문장으로 정한다.
2. 기존 레이아웃·토큰을 우선 사용한다. 새로운 색·radius·장식은 이유 없이 추가하지 않는다.
3. 성공 화면만 만들지 않는다. 로딩·빈 상태·실패·오래된 근거·수동 확인·권한 부족도 설계한다.
4. 실제 데이터 흐름을 연결하고 키보드·반응형·오류 경로를 검증한다.
5. 브라우저 스크린샷을 열어 읽기 순서·밀도·잘림·금지 패턴을 눈으로 확인한다.
6. 테스트 결과, 확인한 viewport, 미검증 접근성 항목을 기록한다. 자동 테스트만으로 전체 UX가 검증됐다고 하지 않는다.

예외가 필요하면 변경 설명에 **사용자 작업 / 기본 대안이 부족한 이유 / 적용 범위 / 검증 방법 / 제거 조건**을 적는다. 사용자 금지사항을 변경하는 예외는 먼저 사용자에게 확인한다.

## 9. 현재 구현에서의 적용과 남은 점

- 카드 모자이크·외부 폰트·blur를 사용하지 않는다. 주요 버튼의 절제된 광택 gradient만 사용자 요청에 따라 허용한다.
- 세션·보고서·경고의 측면 강조선을 제거한다. 상태 표시는 평평한 텍스트로 통일한다.
- 데스크톱과 모바일 실브라우저 캡처를 테스트에 포함한다.
- 상세 설정과 시나리오 JSON 편집은 alpha의 기능적 UI다. 향후 전용 편집 화면으로 분리할 후보이며 완성된 비개발자 UX로 주장하지 않는다.
- 스크린 리더 실사용 평가, 전체 대비 자동 감사, 모든 화면의 200% 확대는 별도의 검수 항목이다. 기능 테스트 통과로 대신하지 않는다.
