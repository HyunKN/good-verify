# good-verify working agreements

- UI 작업 전 [Design.md](Design.md)를 읽고 따른다. 사용자 금지 패턴을 템플릿이나 skill의 시각적 취향으로 덮어쓰지 않는다.
- TypeScript Core는 프로젝트·브라우저·저장소 구현에 의존하지 않는다. Application과 adapter의 경계를 지킨다.
- 연결 대상의 이름·절대 경로·전용 로직·실제 기록·자료는 이 저장소에 넣지 않는다. 범용 샘플과 테스트만 사용한다.
- 로컬 기록은 OS 사용자 데이터 공간에 보관한다. 비공개 브라우저 상태를 보고서·테스트·공유 설정에 포함하지 않는다.
- 기대 결과를 실행하지 않았거나 수동 확인이 남아 있으면 자동 통과로 처리하지 않는다. 모든 재시도와 당시 코드·시나리오 정의를 보존한다.
- 기존 변경을 보존하고 외부 시스템에 자동 게시하거나 push하지 않는다.
- 검증: `pnpm verify`, 브라우저 관련 변경은 `pnpm test:e2e`. UI 변경은 캡처를 직접 확인한다.
- 실제 확인한 환경과 미검증 범위를 구분한다. 자체 테스트 통과를 연결 대상 전체 또는 실계정 검증으로 표현하지 않는다.
